1. when user1 called user2, and user2 does not answer. Then, user2 call user1, user1 gets 
error messages like this:
server disconnected
call not answered
server disconnected
call not answered

Possible fix: debug from server recieving side, because client side prints server disconnected,
that triggered by no data sent from server. So, print the command this client sent to server,
and see what is happeneing
###FIXED###
2. when user ended call the subprocess is still runnin, which casues the mic is alwasy occupied.
need to fix the subprocess problem
